<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dd.pgadmin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/settings"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PGAdmin">
        <activity android:name=".Booking_adapter"></activity>
        <activity android:name=".ViewActivity_adapter" />
        <activity android:name=".registeration_act" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewActivity" />
        <activity android:name=".BookingDetails" />
        <activity android:name=".EditActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>